# Configuration Settings

# Unique identifier for the agent - get from http://localhost:3000/agents
# If not provided, defaults to "default"
AGENT_ID=your-agent-id-here

# All fields below have default values and can be customized as needed

# Number of initial tokens allocated to each room (default: 10000)
INITIAL_TOKENS=

# Time period in hours before token count resets (default: 5)
RESET_HOURS=

# Port where your target API server is running (default: 3000)
TARGET_SERVER_PORT=

# Port for the rate limiter service to run on (default: 3003)
PORT=

# Path to store the SQLite database (default: ./rate-limiter.db)
DB_PATH=./your-db-name.db

# Removed Debouncer
# Debounce wait time in milliseconds (default: 1000)
# DEBOUNCE_WAIT=